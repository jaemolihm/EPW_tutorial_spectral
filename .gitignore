# LaTeX build artifacts
*.aux
*.bbl
*.bcf
*.blg
*.fdb_latexmk
*.fls
*.idx
*.ilg
*.ind
*.lof
*.log
*.lot
*.nav
*.out
*.run.xml
*.snm
*.synctex.gz
*.toc
*.vrb
*.xdv
*.dvi
*.ps

# macOS
.DS_Store

# Editor / misc
*.swp
*~

# Local debugging workspace (mirrors code/ via symlinks; outputs not tracked)
workspace/

# Build output for the handout. To version a PDF, copy it to a tracked path
# (e.g., the repo root or a release/ directory) rather than committing this.
document/main.pdf

# Makefile artifacts (handout copy + code tarball produced by `make`)
Thu.5.Lihm.pdf
Thu.5.Lihm.tar
